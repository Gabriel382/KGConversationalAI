# Hugging Face Spaces installs this on first build.
# Template mode by default; visitors can paste their own OpenRouter key
# in the UI to get LLM responses.
kgconvai[kg] @ git+https://github.com/Gabriel382/KGConversationalAI.git@main
gradio>=5.0
pyvis>=0.3
audioop-lts; python_version >= "3.13"
