# Development dependencies for kgconvai.
# Install with: pip install -r requirements-dev.txt
# (Or, equivalently: pip install -e .[dev])

-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.12
hypothesis>=6.100
ruff>=0.4
mypy>=1.10
types-requests
pre-commit>=3.7
